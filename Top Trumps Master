import random

def gameMenu():
    ans=True
    while ans:
        print(" 1.Play the Game 2.Instructions  3.Exit/Quit")
        
        ans=input("What would you like to do? ")
        if ans=="1":
          name = input("\n What is your name? ")
          print("Hello ", name, ". Let's play Top Trumps!")
          playGame()
        elif ans=="2":
          print("\n Okay then: here are the instructions: ")
          print(gameInstruction)
        elif ans=="3":
          print("\n Goodbye") 
          ans = False
        else:
           print("\n Please Enter an Option Shown")

def my_shuffle():
        array1 = ['a','b','c','d','e','f']
        random.shuffle(array)
        print(array)
        return array

my_shuffle()
